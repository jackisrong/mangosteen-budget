package model;

public class ExpenseCategory extends Category {
    // MODIFIES: this
    // EFFECTS: creates new ExpenseCategory with name name
    public ExpenseCategory(String name) {
        super(name);
    }
}
